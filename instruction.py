class instruction:

    def __init__(self,ins_code, to_address,to_port,data):
        self.ins_code = ins_code
        self.to_address = to_address
        self.to_port = to_port
        self.data = data